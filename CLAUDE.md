# Claude作業ルール

## 基本方針
- 作業前に必ずこのファイルを参照する
- 日本語で回答する
- 変更前に必ず現状のファイルを確認・把握してから作業を開始する
- 実行するコマンドは事前に内容を報告し、承認を得てから実行する

## ワークフロー
作業は以下の順序で行う：

1. ローカル（デスクトップ）で作業・保存　← メインの作業場所
2. 作業内容をGitにコミット（変更履歴を記録）
3. GitHubにpush（クラウドバックアップ）

```
デスクトップで作業 → コミット → GitHubへpush
```

- pushの前に必ず変更内容を報告し、承認を得てから実行する
- コミットメッセージは日本語で簡潔に書く

## ファイル・コードの変更原則

### ファイル削除の禁止
- いかなる場合もファイルの削除は行わない
- 不要になったファイルであっても削除せず、ユーザーに判断を委ねる

### バージョン管理
- コードの削除・編集を行う場合は、Gitのコミット履歴で過去バージョンを遡れるようにする
- 手動でのバージョン管理（フォルダ名やファイル名にv1.0などをつける方法）は行わない
- GitHubへのpush前に変更内容を必ず報告し、承認を得てから実行する

### 機密情報・個人情報の保護
- .envや機密情報を含むファイルは絶対にGitHubにpushしない
- APIキー・パスワード・認証情報をコードに直接書かない
- 個人情報や業務データが含まれている可能性があるファイルは、push前に必ずユーザーに確認する
  - 例：氏名・住所・連絡先・金額などが含まれるExcelやCSVファイル

## 新しいプロジェクト開始時のルール
- 新しいプロジェクトフォルダを作成したら、必ずそのフォルダ内にCLAUDE.mdを作成するようユーザーに進言する
- CLAUDE.mdに記載する内容：
  - プロジェクト概要
  - GitHubリポジトリのURL（SSH）
  - 現在の状態・経緯
  - 今後の方針
  - ファイル構成
  - 作業時の注意点
- プロジェクト内に `.claude/skills/log/SKILL.md` を作成する（Git経由で新PCにも共有される）

## 新PCセットアップ手順
Claude Codeを新しいPCで使い始める際は以下を実施する：

1. 個人用Skillを作成する
```
mkdir -p ~/.claude/skills/log
```
2. 既存プロジェクトの `.claude/skills/log/SKILL.md` の内容を `~/.claude/skills/log/SKILL.md` にコピーする
3. 各プロジェクトを `git clone` または `git pull` で取得する

### Skillの管理方針
- `~/.claude/skills/log/SKILL.md`：個人用（このPC上の全プロジェクトで使える）
- `.claude/skills/log/SKILL.md`：プロジェクト用（GitHubで共有・新PCに自動配布される）
- 両方を維持することで、どの環境でも `/log` が使えるようにする

## 作業前のリスク評価

作業を開始する前に、以下の観点でリスク評価を行いユーザーに提示する：

- **担当の判断**：各作業をClaudeが実行すべきか、ユーザーが行うべきか明示する
  - ローカル（PC上）での作業 → Claudeが実行可
  - GitHubなど外部サービスのWebサイト操作・ログイン・設定変更 → ユーザーが行う
- **リスクの明示**：取り消せない操作・機密情報に関わる操作は特に明示する
- **承認の取得**：評価結果をユーザーに提示し、進め方の合意を得てから作業を開始する

## 作業報告のルール
- 作業開始前：何をするか報告する
- 作業中：問題・疑問が生じたら即座に報告し作業を止める
- 作業完了後：何を変更したか報告する

## 核心原則
1. 最小性：必要最小限を超えない
2. 確認優先：迷ったら作業を止めて確認する
3. 透明性：失敗や問題は即座に報告する
4. 不可逆操作の禁止：削除など取り消せない操作は行わない

## 作業記録

作業終了時に `/log` コマンドで記録を残す。
別のPCで作業を引き継ぐ際は、この記録を参照してコンテキストを把握すること。

### 記録フォーマット
```
### YYYY-MM-DD
- 作業内容：
- 決定事項：
- 次のタスク：
- 注意事項：
```

### 記録欄
<!-- 以下に作業記録を追記していく -->

### 2026-05-12

#### 背景と出発点
学校の朝の会がマンネリ化し、子どもたちの集中力が低下しているという課題があった。朝の会（号令・挨拶・日付天気・出席・給食・歌・日直の話・先生の話の8ステップ）をアプリ化することで、楽しく主体的に運営・参加できる環境を作ることが目的。ゼロからの新規プロジェクト立ち上げ。

#### 技術スタックの選定
WebのHTML/CSS/JavaScriptとReactの違いをユーザーが理解していなかったため、まず違いを説明した。今回の規模（8ステップのミニアプリ集）ではReactは過剰であり、ブラウザで直接開けるHTML/CSS/JSが最適と判断。iPadのSafariで動作・PCで開発・管理という環境要件も確認した。

天気APIについてはOpen-Meteo（完全無料・登録不要）を採用。料金の懸念を持っていたユーザーに対し、月1,000回制限のOpenWeatherMapではなく制限なしのOpen-Meteoを推奨した。オフライン対応はlocalStorageによる前回データのキャッシュで実現することを決定した。

#### アプリ設計の決定事項
ユーザーの要望を整理し、以下の仕様を確定した：
- 画面構成：スタート画面 → メイン画面 → 終了画面（10秒で自動スタート戻り）
- メイン画面：左に垂直タブ（朝の会の流れ）・右にミニアプリ表示エリア
- ナビゲーション：右下に「次へ」・左下に「もどる」ボタン・中央にプログレスドット
- 管理画面：右下の隠しエリアを3回タップ → パスワード入力（初期値：1234）で入室
- デザイン：朝らしい暖色系・親しみやすい・派手すぎない・iPad横画面最適化
- 各ステップはミニアプリとして独立させ、今後一つずつ開発していく方針

#### フォルダ作成・ファイル実装
`/Users/nagaiyushi/Desktop/Claude/朝の会アプリ/` を作成し、3ファイルを実装：
- `index.html`：8ステップ分のミニアプリ枠・管理画面モーダル・全画面構造
- `style.css`：CSS変数によるカラーパレット管理・アニメーション・レスポンシブ対応
- `script.js`：ステップ管理・プログレスドット・天気API取得・管理画面認証ロジック

現時点で④出席・⑤給食・⑥歌・⑦日直の話・⑧先生の話のミニアプリはプレースホルダー表示。①号令・②挨拶・③日付天気は基本実装済み。

#### Git・GitHub連携
- `git init` でローカルGit管理を開始（他プロジェクトへの影響なし）
- 初回コミット完了（コミットID: d0dbab4）
- GitHubリポジトリ作成はユーザーが実施：`morning-assembly`（Private）
- SSH URL：`git@github.com:aquariaeuro0126-cyber/morning-assembly.git`
- `git remote add origin` → `git push -u origin main` でアップロード完了
- SSHキーはPC単位で設定済みのため、リポジトリごとの設定は不要であることをユーザーに説明

#### 決定事項・次のタスク
- 管理画面の機能は各ミニアプリを開発しながら都度追加していく方針
- GitHubリポジトリはPrivate設定（学校関係データの保護）
- 次のタスク：各ミニアプリの中身を一つずつ実装していく（どの項目から着手するかはユーザーと相談）
- プロジェクト内CLAUDE.mdの作成がまだ未実施（CLAUDE.mdルールに従い、次回作業開始時に対応を進言すること）

### 2026-05-12（第2セッション：①号令・②挨拶・③日付天気ミニアプリの実装と修正）

#### 背景と出発点
第1セッション（プロジェクト立ち上げ）でアプリの骨格（HTML/CSS/JS）は完成していたが、
各ミニアプリの中身はすべてプレースホルダー表示だった。
第2セッション（実際は複数回のセッションにまたがる）で①〜③の実装を行った。

#### ① 号令ミニアプリの実装
朝の会の最初の儀式「気をつけ → 礼 → 直れ」をアプリ化。
フェーズ構造（ready → kiotsuke → rei → done）で管理し、各フェーズを `.reiko-phase.hidden` で切り替える方式を採用。

- **フェーズ①（ready）**：「🔔 気をつけ！」ボタンを表示
- **フェーズ②（kiotsuke）**：「気をつけ！」の大テキスト + SVGカウントダウンリング（3秒）
  - SVGの `stroke-dashoffset` をCSSアニメーションで0→326.7へ変化させることで円形プログレスを実現
  - 3秒後に自動でフェーズ③へ遷移
- **フェーズ③（rei）**：「礼！」の大テキスト（橙色）+ 「直れ！」ボタン
  - 「礼！」はフォントサイズを96pxに大きくし、子どもが視認しやすく
- **フェーズ④（done）**：「✅ 号令おわり！」+ 「もう一度」ボタン

#### ② 挨拶ミニアプリの実装（音声測定）
「おはようございます！」を元気よく言えるかを音声で測定するミニアプリ。
Web Audio API（getUserMedia + AudioContext + AnalyserNode）を使用。

- **フェーズ①（ready）**：「🎤 はかってみる！」ボタン
- **フェーズ②（countdown）**：3秒カウントダウン（大きな数字表示）
- **フェーズ③（measuring）**：
  - 「おはようございます！」の掛け声テキスト表示
  - 音量メーターバー（リアルタイム更新、グラデーション表示）
  - 3秒間測定後に自動で結果へ
- **フェーズ④（result）**：0〜100点のスコア + メッセージ表示
  - スコア算出：測定中の最大音量（0〜1.0）を100点満点に換算
  - メッセージは点数帯ごとに変化（80点以上：「すごい！」など）

**設計の判断**：
- マイクのパーミッション取得は「はかってみる！」ボタン押下時に行う（初回から要求すると子どもが驚くため）
- Safari（iOS）はWebAudio APIの挙動がChromeと異なるため、`AudioContext` の `resume()` を明示的に呼ぶ

#### ③ 日付・曜日・天気ミニアプリの実装
3フェーズ構造：「日付選択 → 天気クイズ → まとめ表示」

**フェーズ①（date）：スライダー式日付選択**
- 月（1〜12）・日（1〜31）・曜日（日〜土）をドラム式▲▼ボタンで選択
- 循環インデックス（`wrapIdx`関数）で端まで来たら反対側に戻る
- 初期値はランダム（ランダムにすることで「正解探し」の意欲を引き出す）

**フェーズ②（weather）：天気クイズ**
- 確認ボタン押下時にOpen-Meteo APIで今日の天気を非同期取得
- 晴れ/くもり/雨/雪の4択ボタンを表示
- 選択後、正解（APIの天気）と比較して○×を表示
- 正解ボタンに `.correct`、不正解に `.wrong` クラスを付与してスタイリング
- 「次へ」ボタンが現れてフェーズ③へ進める

**フェーズ③（summary）：まとめ表示**
- 「今日は○月○日（○曜日）天気は○○です！」のまとめを大きく表示
- 「もう一度」でフェーズ①に戻りリセット

**Open-Meteo API連携**：
- `https://api.open-meteo.com/v1/forecast` を使用（無料・登録不要）
- WMO天気コードを日本語天気名（晴れ/くもり/雨/雪）にマッピングする関数を実装
- 位置情報はブラウザの `navigator.geolocation` で取得
- localStorageに当日の取得結果をキャッシュし、オフライン・API障害時にもデータを再利用

#### 遭遇した問題と解決

**問題①：月スライダーの消失**
- 症状：フェーズ①のスライダーが日・曜日の2列のみで月がなかった
- 原因：UI修正作業中に月スライダーのHTMLが誤って削除された
- 修正：`index.html` に月スライダー列を再追加、`script.js` にMONTHS配列・monthIdx・selectedMonth等を復元
- コミット：`10ebd3f`

**問題②：確認ボタンを押してもフェーズが切り替わらない**
- 症状：確認ボタンを押しても天気クイズへ移行しない
- 原因究明：`showDwPhase()` は正常に `hidden` クラスを付与していたが、`style.css` に `.dw-phase.hidden { display: none; }` が存在しなかった。`.voice-phase.hidden` や `.reiko-phase.hidden` は存在していたのに③だけ抜けていた
- 修正：`style.css` に `.dw-phase.hidden { display: none; }` を追加
- コミット：`e1c0247`
- 教訓：フェーズ管理パターン（hiddenクラス切り替え）を使う際は必ずCSSの `.クラス名.hidden { display: none; }` とセットで実装する

#### 決定事項・次のタスク
- ①号令・②挨拶・③日付天気ミニアプリは正常動作する状態になった
- フェーズ管理のCSS忘れ防止：今後の実装時は `.フェーズクラス.hidden { display: none; }` の追加を必ず確認する
- 次のタスク：④出席・⑤給食・⑥歌・⑦日直の話・⑧先生の話のミニアプリ実装（現在プレースホルダー表示のまま）

### 2026-05-12（第3セッション：④〜⑦ミニアプリの実装記録）

#### 背景と出発点
第2セッションで①〜③の実装が完了した。本記録は④出席・⑤給食・⑥歌・⑦日直の話の実装について、
コードを基に詳細を整理したものである。

#### ④ 出席ミニアプリの実装

3フェーズ構成（ready → calling → result）で実装した。

**フェーズ①（ready）：スタート画面**
- localStorage から名前リスト（`att_names`）を読み込み、登録人数を表示
- 名前が0人の場合はエラーメッセージ表示・スタートボタン無効化

**フェーズ②（calling）：呼名画面**
- 名前リストを Fisher-Yates アルゴリズムでシャッフルし、1人ずつ大きく表示
- 「✅ 出席」「❌ 欠席」ボタンで回答 → 次の人へ自動遷移
- 「○ / 全体」の進捗テキスト表示
- 名前カードが切り替わるたびに `animation: none` → 空フレーム → リセットでアニメーション再起動

**フェーズ③（result）：集計結果**
- 全員出席 → 🌟「全員出席！」表示
- 欠席者あり → 📋「出席○人 / 欠席○人」 + 欠席者名一覧表示
- 「もう一度」で①に戻りリセット

**管理画面（名簿管理）**
- 名前の追加・削除・全削除機能
- localStorage `att_names`（JSON配列）に永続保存
- `roster-updated` カスタムイベントで本体ミニアプリへデータ変更を通知
- 管理画面を開くたびにリストを最新化（`admin-opened` イベント）

**設計の判断**：
- シャッフルは毎回新規 → 同じ子どもが連続で呼ばれることを防ぐ
- 名前データの保持はlocalStorageのみ（年度をまたぐ際は先生が手動で更新）

#### ⑤ 給食ミニアプリの実装

3フェーズ構成（ready → quiz → menu）で実装した。

**フェーズ①（ready）：スタート画面**
- 今日の曜日（月〜金）を自動判定して表示（`new Date().getDay()`）
- 今日の曜日の献立が未登録の場合はスタートボタン無効化
- 土日は「今日の献立はありません」と表示

**フェーズ②（quiz）：献立当てクイズ**
- 今日の献立からランダムに1品を正解として選出
- ダミー20種類（カレー、スパゲッティ等の定番メニュー）の中から3品をランダム選出して4択を構成
- ダミーは「今日の献立に含まれていない」ものだけを候補とする（重複排除）
- 正解・不正解に応じてボタンに `.correct` / `.wrong` クラスを付与
- 回答後、正解ボタンも常にハイライト表示（不正解時に正解を学べる）

**フェーズ③（menu）：献立一覧表示**
- 今日のメニューを全品リストアップ（アイコン付き）
- アニメーションに `animationDelay` を品目番号×0.08秒でずらして段階表示

**管理画面（給食献立管理）**
- 月〜金の5曜日分のカード表示（未登録は「未登録」表示、登録済みはチップ表示）
- 各曜日の「編集」ボタンで最大8品まで入力できる編集エリアを開く
- 保存時は空欄を除外して配列化、localStorageの `kyushokuMenus` に保存
- `document.dispatchEvent(new CustomEvent('roster-updated'))` で本体ミニアプリへ通知

**設計の判断**：
- 週単位でメニューを事前登録するワークフロー（毎日変更ではなく週1回の運用）
- ダミーを固定の20種類から選ぶことで、常に子どもになじみのある選択肢が並ぶ

#### ⑥ 歌ミニアプリの実装

4フェーズ構成（ready → lottery → select → play）で実装した。
YouTube IFrame API を利用してブラウザ内で動画再生。

**フェーズ①（ready）：スタート画面**
- 「今月の歌」のタイトルを表示（未登録の場合はスタートボタン無効化）
- 「くじをひく！」ボタンでフェーズ②へ

**フェーズ②（lottery）：くじ引き**
- くじボックスが左右に揺れる `lotteryShake` アニメーション（1.5秒）
- 当たり確率 = 管理画面で設定した「当たり枠数（1〜9）/ 10」で計算（`Math.random()`）
- 当たり → 「🎊 当たり！！好きな歌を選んでいいよ！」→ フェーズ③（曲選択）へ
- はずれ → 「😢 はずれ…今月の歌を歌おう！」→ フェーズ④（再生）へ直行

**フェーズ③（select）：好きな曲選択**
- 管理画面で登録した「好きな曲リスト」を一覧表示
- 好きな曲が0件の場合は今月の歌を再生（フォールバック）
- 選択するとフェーズ④へ

**フェーズ④（play）：再生画面**
- 曲タイトル・歌詞テキスト・YouTube埋め込みプレーヤーを表示
- `YT.Player` API が利用可能ならIFrame API経由で生成、未読み込みの場合は `<iframe>` を直接埋め込むフォールバックを実装
- YouTubeのURLは `youtu.be/`・`watch?v=`・`embed/` の3パターンに対応する正規表現でIDを抽出

**管理画面（歌設定管理）**
- 「今月の歌」：タイトル・URL・歌詞テキストを設定・保存
- 「当たり枠数」：1〜9の範囲でくじの当たり確率を調整
- 「好きな曲リスト」：複数曲を登録・削除。タイトル・URL・歌詞を1曲ずつ追加

**設計の判断**：
- くじ引き要素を入れることで、毎日同じ曲になるマンネリを防ぐ
- 当たり確率を管理画面で調整可能にすることで、先生がクラスの状況に応じて運用を変えられる
- YouTube IFrame APIの読み込みタイミング問題を `ytPlayerReady` フラグで管理し、
  APIが未準備の場合も `<iframe>` 直書きで対応できるよう二重対応

#### ⑦ 日直の話ミニアプリの実装

3フェーズ構成（ready → spinning → result）で実装した。
ルーレットで「今日の話題」をランダムに決めるミニアプリ。

**フェーズ①（ready）：スタート画面**
- 現在登録中のトピック一覧をチップ形式でプレビュー表示
- 「ルーレットをまわす！」ボタンでフェーズ②へ

**フェーズ②（spinning）：ルーレット演出**
- トピックを縦に並べたドラム式リスト（`.roulette-drum`）をJavaScriptで上方向にスクロールさせる疑似ルーレット
- 5周分のアイテムを生成してスクロール感を演出（`REPEAT = 5`）
- 当選アイテムを事前にランダム決定（`winIndex`）し、最終周のその位置で止まるよう移動距離を計算
- `requestAnimationFrame` ベースの手動アニメーション（加速→減速フェーズ）
  - 加速フェーズ：30フレームかけて初速8から最高速40へ線形加速
  - 減速フェーズ：残り距離が `ITEM_HEIGHT × topics.length` 以下になったら徐々に減速
- 上下に半透明のグラデーションフェード（`::before / ::after` 疑似要素）で「窓から見えている」感を演出
- 中央のハイライトライン（`.roulette-pointer`）で選択位置を強調

**フェーズ③（result）：結果表示**
- 当選トピックを大きく表示（36px太字、ピンク背景ボックス）
- `scorePopIn` アニメーションでポップイン演出
- 「もう一度回す」でフェーズ①に戻る

**管理画面（日直トピック管理）**
- トピックの追加・削除・デフォルトに戻す機能
- デフォルト5題：「今日楽しみなこと」「最近はまっていること」など
- localStorage `nichokuTopics` に保存、`nichoku-updated` カスタムイベントで本体へ通知
- Enterキーでもトピック追加が可能

**設計の判断**：
- ルーレット演出は `requestAnimationFrame` で自前実装（CSSアニメーションだと終了位置の制御が難しいため）
- トピックはデフォルト5件を用意することで、初期設定なしでも即使用可能にした

#### 決定事項・次のタスク
- ④〜⑦の全ミニアプリは正常動作する状態で実装された
- ⑧先生の話も同セッション系列で実装完了（コミット `7bd090c`）、全8ステップが出揃った
- 今後の方針：実際に教室で使用してみてフィードバックをもとに改善していく
- 次のタスク：実運用後の改善要望への対応（現時点では未定）

### 2026-05-12（第4セッション：歌ミニアプリの月別管理・音声ファイル対応）

#### 背景と出発点
前セッションまでで全8ミニアプリの実装が完了し、実運用フェーズに入っていた。
ユーザーから「歌を月ごとに登録・管理したい」「MP3などを直接アップロードして再生できるようにしたい」という改修要望があり、歌ミニアプリ（initSong / initAdminSong）を全面的に書き換えた。

#### データ設計の変更
- 変更前：`songMonthly = { title, url, lyrics }`（1曲のみ）
- 変更後：`songMonthlyAll = { "1": {...}, ..., "12": {...} }`（月番号をキーとするオブジェクト）
- 音声バイナリはlocalStorageの5MB制限を回避するため、IndexedDB（`MorningAssemblyDB` / ストア名 `songAudio`）に分離保存
- 旧データ（`songMonthly`）は削除せず、初回起動時に今月のエントリとして `songMonthlyAll` へ自動マイグレーション

#### IndexedDB ラッパー関数の追加（script.js）
`extractYouTubeId` より前に4関数を追加：
- `openSongAudioDB()`：DB接続・バージョン管理
- `saveSongAudio(monthKey, base64)`：音声データ保存
- `loadSongAudio(monthKey)`：音声データ読み込み
- `deleteSongAudio(monthKey)`：音声データ削除

#### initSong() の書き換え（script.js）
- ストレージキーを `songMonthly` → `songMonthlyAll` に変更
- `loadMonthly()` を「現在月のエントリを自動取得」する実装に変更
- `playSong()` を async 化：YouTube優先 → 音声ファイルフォールバックの条件分岐
- 「もう一度」ボタンに `<audio>` 停止処理を追加
- `initSong()` 先頭に旧データマイグレーション処理を追加

#### initAdminSong() の書き換え（script.js）
- 月タブ（1〜12月）をJSで動的生成し、タブ切り替えでフォームデータを更新
- 保存時は選択中の月キーで `songMonthlyAll` を更新
- MP3アップロード：FileReader で base64 変換 → IndexedDB に保存
- 音声ファイル登録済みの場合「✅ 登録済み」、未登録の場合「未登録」と表示
- 削除ボタンで IndexedDB から音声データを削除
- ファイルサイズ30MB超の場合アラートで警告

#### index.html の変更
- 再生フェーズ（`song-phase-play`）内に `<audio id="song-audio-player">` を追加
- 管理画面の歌セクションを月タブUI付きフォームに全面置き換え
  - `id="admin-song-month-tabs"` — JSで1〜12月タブを動的生成
  - `id="admin-song-m-audio"` — `accept="audio/*"` ファイル選択
  - `id="admin-song-m-audio-status"` — 登録状態表示
  - `id="btn-admin-song-m-audio-delete"` — 音声削除ボタン

#### style.css の追加
- `.admin-month-tabs` / `.admin-month-tab` / `.admin-month-tab.active`：月タブのスタイル
- `.admin-song-m-audio-row` / `.admin-song-file-input` / `.admin-song-audio-status`：音声入力エリア
- `.btn-sm`：小さいボタン汎用クラス
- `#song-audio-player` / `#song-audio-player.hidden`：音声プレーヤー

#### 決定事項・次のタスク
- コミット `eb1aafe`（3ファイル、+416行/-122行）をGitHubにpush済み
- 好きな曲リスト（`songFavList`）はYouTube URLのみ対応のまま（音声ファイル非対応）
- 次のタスク：実際に使用してフィードバックをもとに改善（現時点では未定）

### 2026-05-12（第5セッション：出席ミニアプリへの先生呼び出し機能の追加）

#### 背景と出発点
出席ミニアプリは「生徒の呼名 → 集計結果」の2フェーズで動作していたが、
ユーザーから「先生の名前も呼べるようにしたい。複数名の先生を登録し、呼ぶ順番を設定できるようにしてほしい」という要望があった。
前セッション末に要望が途中で切れていたため、画面仕様を確認してから実装に着手した。

#### 仕様の確認と設計方針
ユーザーへの確認で以下の仕様が決定した：
- 先生呼び出し時は出席ボタン不要。名前を大きく表示するだけでよい
- 「せーの！」をオレンジ色・大フォントで表示する
- 「次へ」ボタンで次の先生に進む
- フロー順序は「生徒フェーズ → 先生フェーズ → 集計結果」の順（実装中にユーザーから追加指定）

**フロー構成**：
①ready → ②calling（生徒呼名）→ ③teacher（先生呼び出し）→ ④result（集計結果）

**先生が0人の場合の対応**：
teacherフェーズをスキップして結果へ自動遷移するロジックを追加。
管理画面で先生を未登録のまま使っても問題なく動作する。

#### index.html の変更
- フェーズ③（先生呼び出し）を `att-phase-calling` の直後・`att-phase-result` の直前に追加
  ```html
  <div class="att-phase hidden" id="att-phase-teacher">
    <div class="att-teacher-label">先生（せんせい）</div>
    <div class="att-teacher-name-card">
      <div class="att-teacher-name" id="att-teacher-name"></div>
    </div>
    <div class="att-senno-wrap">
      <div class="att-senno" id="att-senno">せーの！</div>
    </div>
    <button id="btn-att-teacher-next" class="btn-att-teacher-next">次へ（つぎへ）→</button>
  </div>
  ```
- 管理画面に「先生の名前」登録ブロック（`admin-teacher-block`）を追加
  - `id="admin-teacher-input"`：名前入力欄
  - `id="btn-admin-add-teacher"`：追加ボタン
  - `id="admin-teacher-list"`：登録済み先生一覧
  - `id="admin-teacher-count"`：登録人数表示
  - `id="btn-admin-clear-teachers"`：全員削除ボタン
- 児童名前ブロックも同じ `admin-teacher-block` クラスで統一

#### script.js の変更（initAttendance）
- `phaseTeacher`・`teacherNameEl`・`btnTeacherNext` の参照を追加
- `showAttPhase()` に `phaseTeacher` を追加（全フェーズを一括で hidden にして切り替え）
- 生徒全員の呼名が完了した際の処理を変更：
  - 先生リスト（`teacher_names`）を localStorage から取得
  - 先生が0人 → 直接 `showResult()`
  - 先生が1人以上 → `showTeacherCard()` を呼んで `phaseTeacher` を表示
- `showTeacherCard()`：名前カードのアニメーションをリセットしてから表示
- `btnTeacherNext` クリック：`teacherIndex` を進め、全員終了したら `showResult()`

#### script.js の変更（initAdminRoster）
- 先生名管理のロジックを追加（`loadTeachers()`・`saveTeachers()`・`renderTeacherList()`）
- localStorage キー：`teacher_names`（JSON配列）
- 先生リストは番号付き表示（例：「1. 山田先生」「2. 田中先生」）
- 追加・削除・全削除・空欄バリデーションを実装
- `roster-updated` カスタムイベントで本体ミニアプリへ通知

#### style.css の変更
先生呼び出しフェーズとその管理UIのスタイルを追加：
- `.att-teacher-label`：「先生」ラベル（漢字・ひらがなの二段表示）
- `.att-teacher-name-card`：名前カードのアニメーション付きコンテナ
- `.att-teacher-name`：52px・ネイビー・太字
- `.att-senno-wrap`：「せーの！」の中央配置ラップ
- `.att-senno`：72px・オレンジ（`#e65100`）・太字・文字間隔4px
- `.btn-att-teacher-next`：次へボタン（青系グラデーション）
- `.admin-teacher-block`：管理画面の先生名登録ブロック（水色背景）
- `.admin-teacher-block-title`：ブロックタイトル

#### 遭遇した問題と解決

**フロー順序の途中変更**
- 症状：当初「先生 → 生徒」の順で実装を開始したが、作業中にユーザーから「生徒フェーズ入力後、先生フェーズに入るように」との指示があった
- 対応：HTMLのフェーズ順序を `calling → teacher → result` に並べ直し、JSの遷移ロジックも「生徒全員完了後に先生フェーズへ移行」するよう修正
- 教訓：フロー順序など仕様の根幹部分は実装開始前に確認しておくとよい

#### 決定事項・次のタスク
- コミット `940da15`（feat: 出席ミニアプリに先生呼び出し機能を追加）をGitHubにpush済み
- 先生フェーズは「ボタンなし・大きな名前表示・せーの！」というシンプルな仕様に決定
- 先生が未登録の場合は先生フェーズをスキップする設計を採用（後方互換性確保）
- 次のタスク：実際に使用してフィードバックをもとに改善（現時点では未定）

### 2026-05-28（朝の会アプリ：各ミニアプリの改善・歌デフォルト登録）

#### 背景と出発点
朝の会アプリが実際に使えるようになったあと、iPadでの操作性改善・給食ミニアプリのUI見直し・歌ミニアプリのデフォルト曲登録など、複数の改善要望が上がったため対応した。

#### ②挨拶ミニアプリ：すわりましょう。カウントダウンの追加

**要望**：点数発表後、全員が着席するまでの間「すわりましょう。」を自動表示したい。

**実装**：点数を表示してから3秒カウントダウン（3→2→1）を経て「すわりましょう。」を大きくポップイン表示する仕組みを追加。
- `index.html`：`#voice-suwarimashoo-wrap`・`#voice-suwarimashoo-count`・`#voice-suwarimashoo-text` 要素を追加
- `script.js`：`showResult()` 内に `setInterval` ベースのカウントダウンロジックを追加。リセット時は `clearInterval` でタイマーを停止
- `style.css`：`.voice-suwarimashoo-wrap`・`.voice-suwarimashoo-count`・`.voice-suwarimashoo-text` のスタイルを追加（`countPop` アニメーションで数字が弾む、`scorePopIn` で「すわりましょう。」がポップイン）

#### ③日付天気ミニアプリ：ドラム式スライドUIへの変更

**要望**：iPadの▲▼ボタン操作でダブルタップ拡大が発生してしまうため、スライド操作方式に変えたい。

**設計の考え方**：CSSの `touch-action: manipulation` や `user-select: none` のみでは解決しなかったため、タッチ操作を完全に自前実装するドラム式UIに変更した。

**実装**：
- `index.html`：月・日・曜日の各スライダーを `.dw-drum` 要素（矢印アイコン＋値表示）に変更し、上下スライドの操作ヒントを追加
- `script.js`：`touchstart/touchmove/touchend`（`passive: false` で `preventDefault` を有効化）と `mousedown/mousemove/mouseup` の両イベントを実装。しきい値（35px）を超えると1ステップ変化し、離すまで繰り返し変化しない一発変化方式を採用
- `style.css`：`.dw-drum`・`.dw-drum-active`・`.dw-drum-arrow`・`.dw-slide-hint` を追加

#### ⑤給食ミニアプリ：献立確認フロー＆カテゴリー別管理の追加

**要望①**：スタート画面で今日の献立を先に見せてから「確認した！」ボタンでクイズへ進む形にしたい。

**要望②**：管理画面の献立登録を品目の羅列ではなく「主食・おかず・スープ…」のカテゴリー別に整理したい。

**実装**：
- readyフェーズに献立プレビュー（`.kyu-menu-preview`）と「こんだてをかくにんした！」ボタンを追加
- ボタン色をオレンジ系→緑系（`#43a047→#2e7d32` グラデーション）に変更
- `script.js`：`initKyushoku()` を全面書き換え。データ構造を `items[]`（旧）から `categories{}` オブジェクト（新）に変更。`getItems()` 関数で旧データ自動マイグレーションを実装
- `initAdminKyushoku()` を全面書き換え。CATEGORIES配列（7種）をループしてカテゴリー別入力行を動的生成
- `style.css`：`.kyu-menu-preview`・`.kyu-preview-item`・`.kyu-preview-icon`・`.admin-menu-category-row`・`.admin-menu-category-label` を追加

#### 歌ミニアプリ：くじ引きの常時有効化

**要望**：今月の歌が未登録でも、くじ引きだけは引けるようにしたい。

**実装**：`btnLottery.disabled = false` に変更し、常にくじが引けるようにした。くじが「はずれ」かつ今月の歌が未登録の場合は `'lose-no-song'` 分岐で「もどる」ボタンのみ表示するフォールバックを追加。

#### 歌ミニアプリ：デフォルト曲名・もみじ歌詞の登録

**要望**：各月のデフォルト曲名と、著作権フリーの「もみじ」のひらがな歌詞を初期値として登録したい。

**著作権の判断**：
- もみじ（1911年作、作詞：高野辰之、作曲：岡野貞一）→ 著作権消滅・フリーで掲載可
- それ以外の曲（にじ・ビリーブ・世界がひとつになるまで等）→ 著作権あり・歌詞掲載不可
- C案（もみじのみフル歌詞、他曲はタイトルのみ）を採用

**実装**：
- `DEFAULT_MONTHLY_SONGS` 定数（1〜12月の曲名）を `script.js` に追加
- もみじの歌詞（2番まで）を全ひらがなで `lyrics` に設定
- `applyDefaultSongs()` IIFE で「未登録月のみデフォルト補完」する処理を追加（既登録データは上書きしない）

#### 決定事項・次のタスク
- 著作権があるその他の曲の歌詞は登録しない（先生が各自で設定する運用とする）
- 次のタスク：実際に教室で使用してフィードバックをもとに改善（現時点では未定）

### 2026-05-28（第2セッション：歌くじ引き仕様変更＆アニメーション追加・バグ修正）

#### 背景と出発点
歌ミニアプリのくじ引き機能について、2点の改修要望があった。
①「はずれ」という言葉が今月の歌を歌うモチベーション低下につながるためなくしたい、②くじ引き感が薄いのでアニメーションを追加したい、というもの。
実装後にアニメーションが動作しないバグが発生し、原因究明と修正も行った。

#### くじ引きメッセージ変更

**変更内容**：くじ結果を「はずれ→今月の歌を歌おう」から「今月の歌！」「あたり！好きな歌！」の2択に変更。「はずれ」という言葉を完全に削除。
- `lotteryShakeText` の「はずれ…」→「」（非表示）
- `lotteryTicketBack` 裏面テキストを「こんげつのうた！」「あたり！すきなうたをえらぼう！」に変更

#### チケットフリップアニメーションの追加（コミット 92a115c）

**設計の考え方**：箱からチケットを引き出して開封するアニメーションを実現するため、CSSの `perspective` + `rotateY` によるカードフリップを採用。

**実装手順（3段階）**：
1. Stage1（1.5s）：📦 箱が揺れる（`.lottery-box.shaking` アニメーション）
2. Stage2（0.55s）：箱が開く演出 + チケットが浮き出る（`ticketRise` アニメーション、`position: absolute; bottom: 60px` → `translateY(-175px)`）
3. Stage3（0.65s）：チケットがひっくり返る（`.lottery-ticket.flipped` で `rotateY(180deg)` トランジション）
4. Stage4：「次へ」ボタン出現

**HTML変更**：`.lottery-scene` コンテナ（position: relative）にチケット（position: absolute）と箱を配置。`.lottery-ticket` に表面（🎫）と裏面（結果テキスト）の2面構造を追加。

**CSS変更**：`perspective: 900px`・`transform-style: preserve-3d`・`backface-visibility: hidden` でカードフリップを実現。

#### 遭遇した問題と解決（コミット 836a36a）

**症状**：「くじを引いています…」フェーズから先に進まない、アニメーションが正しく表示されない。

**原因究明**：3つの問題を特定した。

**問題①：チケットがクリッピングで消える**
- `position: absolute; bottom: 60px` から `translateY(-175px)` で移動させると、チケットの上端が `.lottery-scene`（高さ260px）の91px上方に飛び出す
- 親の `.mini-app-container { overflow: hidden }` によって91px分がクリッピングされ、チケット上部が見えなかった
- 発見の過程：CSSのレイアウト計算（bottom: 60px + translateY(-175px) = チケット最終bottom位置235px → top: -91px）を手計算して特定

**問題②：Safari（iPad）でカードフリップが機能しない**
- `perspective` プロパティをアニメーション中の `.lottery-ticket-wrap` 自身に設定していた
- 要素に `perspective` と `transform` を同時に持たせると、Safari の 3D レンダリングが不安定になる
- `-webkit-transform-style: preserve-3d` がなく iOS Safari で `backface-visibility` が効かなかった

**問題③：「次へ」ボタンが画面外**
- `.lottery-scene`（260px固定高）+ guide text + button の合計がmini-app-bodyの高さを超えると、ボタンがスクロールしないと見えない位置に出る可能性があった

**解決策**：
- `position: absolute` を廃止し、チケットをフレックスの通常フローに戻す（`.lottery-scene` コンテナも削除）
- `perspective` を専用の静的ラッパー `.lottery-ticket-perspective` に分離（アニメーション対象と3D文脈を分離）
- `-webkit-transform-style: preserve-3d` を追加
- アニメーションを `ticketRise`（絶対配置上昇）→ `ticketPopIn`（スケールポップイン）に変更
- ボタン出現時に `scrollIntoView({ behavior: 'smooth', block: 'nearest' })` を追加

**修正後の動作**：箱が揺れる→チケットが箱の下からスケールアップして現れる→カードがひっくり返って結果表示、の流れで動作することを確認。

#### 決定事項・次のタスク
- コミット `92a115c`（チケットフリップアニメーション追加）・`836a36a`（バグ修正）をGitHubにpush済み
- 「箱から飛び出す」アニメーションはクリッピング問題のためポップイン方式に変更（設計変更）
- `perspective` は必ず静的なラッパー要素に設置し、アニメーション対象要素には置かないことを教訓として記録
- 次のタスク：実際に使用してフィードバックをもとに改善（現時点では未定）
